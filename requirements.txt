# ┌─────────────────────────────────────────────────────────────────────────────┐
# │  Psychotherapy Dataset Curation Pipeline — Python Dependencies              │
# │  Install:  pip install -r requirements.txt                                  │
# │  SpaCy model (required for PII NLP engine):                                 │
# │  python -m spacy download en_core_web_sm                                    │
# └─────────────────────────────────────────────────────────────────────────────┘

# ── Core data handling ────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24

# ── Deep learning backbone ───────────────────────────────────────────────────
torch>=2.0
transformers>=4.30
accelerate>=0.20
sentencepiece>=0.2          # needed for some HuggingFace tokenizers
torchaudio>=2.0             # audio loading for transcription

# ── Dataset loading (HuggingFace Hub) ────────────────────────────────────────
datasets>=2.12

# ── Multilingual audio transcription ─────────────────────────────────────────
faster-whisper>=1.0         # lightweight Whisper wrapper, supports MPS/CPU
soundfile>=0.12
librosa>=0.10

# ── PII detection & anonymization (edge-deployable) ─────────────────────────
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7

# ── NLP utilities ────────────────────────────────────────────────────────────
nltk>=3.8
regex>=2023.5.5
tqdm>=4.65

# ── Quality filtering & evaluation ───────────────────────────────────────────
scikit-learn>=1.3

# ── Optional: LLM judge for conversational quality scoring ───────────────────
# langchain>=0.1            # uncomment if using an LLM judge
